import { type SupportedLanguage } from './types'
import { SUPPORTED_LANGUAGES as languages } from '@/i18n/languages'

// 從統一配置導出，無需手動維護
export const SUPPORTED_LANGUAGES: SupportedLanguage[] = languages
